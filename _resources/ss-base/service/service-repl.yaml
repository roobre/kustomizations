apiVersion: kustomize.config.k8s.io/v1beta1
kind: Replacement
source:
  kind: ConfigMap
  name: meta
  fieldPath: data.port
targets:
  - select:
      kind: Service
      name: _
    fieldPaths:
      - spec.ports.0.port
      - spec.ports.0.targetPort
